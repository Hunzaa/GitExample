# Interview Questions for Git

1. What is the command to move the file to a staging area? 
- git add <filename>

2. What is the command to commit to the repo and create a version?
- git commit -m "message"